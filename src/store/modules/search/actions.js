import * as types from '../../mutation-types'


//搜索结果
/*export const initSearchList = ({ commit, state } , callback) => {

    myApp.requestApi(state.request.forum_list, {}, function(obj){
        const data = obj.data
        commit(types.INIT_SERACHE_RESULTS, data)
        if(callback) callback();
    }, true)

}*/
